# CMD-AI-LOADER
Local AI application (CLI + Ollama compatible API) to run GGUF models via llama-cpp-python: model loading, chat in terminal (AI>), model downloading and endpoints /generate, /chat, /tags, /pull.
